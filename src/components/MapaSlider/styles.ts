import { styled } from "../../styles";

export const SwiperComponent = styled('div', {
    width: '50%',
    height: '50%',
    padding: '2rem',
});

export const SwiperSlideComponent = styled('div', {
    width: '100%',
    height: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignContent: 'center',
    backgroundPosition: 'center',
    backgroundSize: 'cover',
});